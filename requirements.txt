# Core
python-telegram-bot[job-queue]==20.7
python-dotenv==1.0.1

# Data Layer
# yfinance >=1.0 memakai backend curl_cffi (impersonasi fingerprint browser)
# yang jauh lebih jarang kena rate-limit 429 Yahoo dibanding 0.2.x (requests
# polos dengan User-Agent default yfinance yang mudah diblokir).
yfinance>=1.0.0,<2.0
requests==2.31.0
aiohttp==3.9.1
# Streaming harga OANDA real-time (WebSocket)
websockets>=12.0

# AI Engine
# (tidak perlu library tambahan, cukup requests)

# Chart LOKAL (candlestick/line) — pengganti QuickChart eksternal
matplotlib>=3.8,<4.0

# Timezone & JobQueue support
pytz>=2023.3
backports.zoneinfo; python_version < "3.9"

# Error tracking (opsional — aktif otomatis jika SENTRY_DSN diisi)
sentry-sdk>=2.0,<3.0

# Logging
# (built-in logging sudah cukup)
